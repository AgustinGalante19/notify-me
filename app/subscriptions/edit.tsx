import Container from '@/components/ui/container';
import Typography from '@/components/ui/typography';

const EditSubscription = () => {
  return (
    <Container>
      <Typography>EditSubscription</Typography>
    </Container>
  );
};

export default EditSubscription;
