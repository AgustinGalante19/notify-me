import Container from '@/components/ui/container';
import Typography from '@/components/ui/typography';

const AddSubscription = () => {
  return (
    <Container>
      <Typography>AddSubscription</Typography>
    </Container>
  );
};

export default AddSubscription;
